<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
  
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">

  <div class="bg-white rounded-xl shadow-2xl flex w-full max-w-4xl overflow-hidden">
    
 
    <div class="hidden md:flex w-1/2 bg-gray-100 items-center justify-center">
      <img src="/images/logoicon.png" alt="Phone" class="w-2/3" style="width: 480px;">
    </div>
    
  
    <div class="w-full md:w-1/2 p-10">
      <h2 class="text-3xl font-bold text-gray-800 mb-6 text-center">Admin Login</h2>
      
      <form action="/admin/login" method="post" class="space-y-4">
        <input name="email" type="email" placeholder="Admin Email" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required />
        
        <input name="password" type="password" placeholder="Password" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required />
        
        <button type="submit" class="w-full  text-white py-2 rounded-md hover:bg-blue-700 transition"  style="background-color: #ff4646;">Login</button>
      </form>

      <% if (locals.message && message.length > 0) { %>
        <div class="bg-red-100 text-red-700 border border-red-400 px-4 py-3 rounded text-center mt-4">
          <%= message %>
        </div>
      <% } %>

      <div class="mt-4 text-center">
        <a href="/forgot-password" class="text-sm text-blue-500 hover:underline">Forgot password?</a>
      </div>

      <p class="text-center mt-4 text-sm text-gray-600">
        Not an admin? 
        <a href="/login" class="text-blue-600 hover:underline">User Login</a>
      </p>
    </div>
  </div>

</body>
</html>
