<!DOCTYPE html>
<html lang="en">
<head>
  <title>RingZone</title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="description" content="RingZone">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link rel="stylesheet" href="/styles/bootstrap4/bootstrap.min.css">
  <link rel="stylesheet" href="/plugins/font-awesome-4.7.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="/plugins/OwlCarousel2-2.2.1/owl.carousel.css">
  <link rel="stylesheet" href="/plugins/OwlCarousel2-2.2.1/owl.theme.default.css">
  <link rel="stylesheet" href="/plugins/OwlCarousel2-2.2.1/animate.css">
  <link rel="stylesheet" href="/styles/main_styles.css">
  <link rel="stylesheet" href="/styles/responsive.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    
.card.product-card {
  display: flex;
  flex-direction: column;
  height: 100%;
  z-index: 5;
}

.card.product-card .card-body {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.card.product-card .btn {
  display: inline-block;
  width: auto;
}


.product-img {
  height: 220px;
  object-fit: contain;
  width: 100%;
  padding: 10px;
  background-color: #f9f9f9;
  border-bottom: 1px solid #eee;
  border-top-left-radius: 0.375rem;
  border-top-right-radius: 0.375rem;
}


.product-card {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  height: 100%;
  border-radius: 10px;
  overflow: hidden;
  background-color: #fff;
  border: 1px solid #ddd;
  transition: 0.3s ease;
}

.cart-count {
  font-size: 0.7rem;
  padding: 0.15em 0.4em;
}
.product-card:hover {
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
  transform: translateY(-3px);
}


.product-card .card-body {
  padding: 1rem;
  display: flex;
  flex-direction: column;
  flex-grow: 1;
}


.product-card .card-title {
  font-size: 1rem;
  font-weight: 600;
  color: #333;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  margin-bottom: 0.5rem;
}


.product-card .text-primary {
  font-size: 1rem;
  font-weight: bold;
  margin-bottom: 0.5rem;
  color: #007bff !important;
}


.product-card .btn-outline-primary {
  font-size: 0.875rem;
  padding: 6px 12px;
  border-radius: 4px;
}


.product-card .fa-heart {
  font-size: 1.25rem;
  transition: color 0.2s;
}

.product-card .fa-heart:hover {
  color: #dc3545;
}


.text-truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}


.banner_item {
  height: 250px;
  border-radius: 12px;
  background-size: cover;
  background-position: center;
  position: relative;
  overflow: hidden;
  transition: transform 0.4s ease;
  margin-bottom: 20px;
  display: flex;
  align-items: flex-end;
  padding: 20px;
}

.banner_item:hover {
  transform: scale(1.03);
}

.banner_overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(to bottom, rgba(0,0,0,0.3), rgba(0,0,0,0.6));
  z-index: 1;
}

.banner_category {
  position: absolute;
  bottom: 20px;
  left: 20px;
  z-index: 2;
}

.banner_category a {
  background-color: rgba(255, 255, 255, 0.9);
  padding: 10px 20px;
  border-radius: 6px;
  color: #000;
  font-weight: bold;
  font-size: 1rem;
  text-transform: uppercase;
  text-decoration: none;
  transition: all 0.3s ease;
}

.banner_category a:hover {
  background-color: #ff7f50;
  color: #fff;
}


@media (max-width: 768px) {
  .banner_item {
    height: 180px;
  }

  .banner_category a {
    font-size: 0.9rem;
    padding: 8px 16px;
  }
}

@media (max-width: 480px) {
  .banner_item {
    height: 150px;
  }
}
.navbar_menu a {
  font-size: 20px;           
  font-weight: 700;        
  letter-spacing: 1px;       
  color: #222;             
  text-decoration: none;
  padding: 10px 18px;       
  position: relative;
  transition: color 0.3s ease;
}

.navbar_menu a::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -5px;
  width: 0;
  height: 3px;
  background-color: #ff7f50; 
  transition: width 0.3s ease;
}

.navbar_menu a:hover {
  color: #ff7f50;
}

.navbar_menu a:hover::after {
  width: 100%; 
}

.sign-in-link {
  font-size: 16px;
  color: #333;
  text-decoration: none;
  font-weight: 600;
}

.sign-in-link:hover {
  color: #ff7f50;
}


.user-avatar {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  font-size: 18px;
  text-transform: uppercase;
}

.dropdown-content {
  display: none;
  position: absolute;
  background-color: white;
  min-width: 160px;
  box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
  z-index: 1000;
  right: 0;
}

.dropdown-content a {
  color: black;
  padding: 10px 16px;
  text-decoration: none;
  display: block;
}

.dropdown-content a[href="/logout"]:hover {
  background-color: #ff7f50;
  color: white !important;
}


.dropdown:hover .dropdown-content {
  display: block;
}


.timer_num {
  width: 90px;
  height: 90px;
  font-size: 2rem;
  font-weight: bold;
}

.timer_unit {
  font-size: 1rem;
  color: #555;
}

  </style>
</head>

<body >
<div class="super_container">


  <header class="header trans_300" style="position: fixed; top: 0; left: 0; width: 100%; z-index: 100; background: #fff;">


    <div class="top_nav">
      <div class="container">
        <div class="row">
          <div class="col-md-6">
            <div class="top_nav_left">🚚 Free & Fast Delivery on All Mobile Orders Across India</div>
          </div>
          <div class="col-md-6 text-right">
            <div class="top_nav_right">
              <ul class="top_nav_menu">
                <li class="currency">
                  <a href="#">INR<i class="fa fa-angle-down"></i></a>
                  <ul class="currency_selection">
                    <li><a href="#">cad</a></li>
                    <li><a href="#">aud</a></li>
                    <li><a href="#">eur</a></li>
                    <li><a href="#">gbp</a></li>
                  </ul>
                </li>
                <li class="language">
                  <a href="#">English <i class="fa fa-angle-down"></i></a>
                  <ul class="language_selection">
                    <li><a href="#">French</a></li>
                    <li><a href="#">Italian</a></li>
                    <li><a href="#">German</a></li>
                    <li><a href="#">Spanish</a></li>
                  </ul>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="main_nav_container">
      <div class="container">
        <div class="row align-items-center justify-content-between">

         
          <div class="col-auto">
            <div class="logo_container">
               <img src="/images/logoicon.png" alt="Phone" class="w-2/3" style="width: 100px;">
            </div>
          </div>

          <div class="col">
            <nav class="navbar justify-content-center w-100">
  <ul class="navbar_menu">
    <li><a href="/">Home</a></li>
    <li><a href="/shopping-pageList">Shop</a></li>
    <li><a href="/page-about">About Us</a></li>
  </ul>
</nav>

          </div>
          <div class="col-auto">
            <ul class="navbar_user d-flex align-items-center mb-0">

              <% if (locals.user && locals.user !== null) { %>
                <li class="dropdown me-3 position-relative">
                 <%
  const colors = ['#FF5733', '#33B5E5', '#FFB400', '#4CAF50', '#9C27B0', '#3F51B5', '#E91E63', '#795548'];
  const getColorFromName = (name) => {
    const charCodeSum = name
      .toUpperCase()
      .split('')
      .reduce((sum, char) => sum + char.charCodeAt(0), 0);
    return colors[charCodeSum % colors.length];
  };
  const userColor = getColorFromName(locals.user.firstname);
%>

<div class="user-avatar" style="background-color:<%- userColor %>;">
  <%= locals.user.firstname.charAt(0).toUpperCase() %>
</div>



                  <div class="dropdown-content">
                    <a href="/user-profile">Profile</a>
                    <a href="#" onclick="confirmLogout(event)">Logout</a>
                  </div>
                </li>
              <% } else { %>
                <li class="me-3">
                  <a href="/login" class="text-nowrap">
                    <i class="fa fa-sign-in" aria-hidden="true"></i> Sign In
                  </a>
                </li>
              <% } %>
 
              <li class="me-3">
<a href="/wishlist" class="wishlist-navbar-icon position-relative" id="navbarWishlistIcon">
  <i class="fa fa-heart-o" aria-hidden="true"></i>
  <span id="wishlist-count" class="cart-count position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="<%= wishlistLength > 0 ? '' : 'display:none;' %>">
    <%= wishlistLength %>
  </span>
</a>

</li>
              <li class="me-3"><a href="#"><i class="fa fa-search" aria-hidden="true"></i></a></li>
             <a href="/user-cart" class="position-relative">
  <i class="fa fa-shopping-cart" aria-hidden="true"></i>
    
  <span id="cart-count" class="cart-count position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
    <%= cartCount %>
  </span>
</a>

            </ul>
          </div>

        </div>
      </div>
    </div>

  </header>

</div>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  function confirmLogout(event) {
    event.preventDefault();
    Swal.fire({
      title: 'Are you sure you want to logout?',
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#d33',
      cancelButtonColor: '#3085d6',
      confirmButtonText: 'Yes, Logout',
      background: '#fff',
    }).then((result) => {
      if (result.isConfirmed) {
        window.location.href = '/logout';
      }
    });
  }
  async function updateWishlistCount() {
    try {
      const response = await fetch('/api/wishlist-count');
      const data = await response.json();

      const count = data.count;
      const wishlistCountEl = document.getElementById('wishlist-count');

      if (count > 0) {
        wishlistCountEl.textContent = count;
        wishlistCountEl.style.display = 'inline-block';
      } else {
        wishlistCountEl.style.display = 'none';
      }
    } catch (err) {
      console.error('Error fetching wishlist count:', err);
    }
  }

  
  document.addEventListener('DOMContentLoaded', updateWishlistCount);
  

  document.body.addEventListener('click', async function (e) {
    const btn = e.target.closest('.wishlist-btn');
    if (!btn) return;
    const productId = btn.dataset.productId;
    try {
      const response = await fetch('/wishlist/toggle', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({ productId })
      });
      const result = await response.json();
      if (result.success) {
        updateWishlistCount();
        btn.classList.toggle('active');
      } else {
        alert(result.message || 'Could not update wishlist');
      }
    } catch (err) {
      console.error('Error updating wishlist:', err);
    }
  });
</script>
</body>
</html>
